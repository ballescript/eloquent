let range = (start, end, step=1) => {
  let numbers = [];
  for(let i = start; i <= end; i += step) {
    numbers.push(i)
  }
  return numbers;
}

let sum = numbers => {
  let result = 0;
  for(number of numbers) {
    result += number
  }
  return result;
}
